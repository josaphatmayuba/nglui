---
"nglui": minor
---

Add `nglui/ui`: a React component set (`Navbar`, `Card`, `Badge`, `Button`, `Input`, `Alert`, `Footer`) with institutional design tokens. `react` is an optional peer dependency required only for this entry point.
